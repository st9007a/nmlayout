<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nm.nmlayout.nmlayout">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DebugActivity" />
        <activity android:name=".HistoryActivity" />
        <activity android:name=".BlockedNotificationsActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AppendToGroupsActivity" />
        <activity android:name=".SchedulePoliciesActivity" />
        <activity android:name=".NewScheduleActivity" />
        <activity android:name=".RegisteredGroupsActivity" />
        <activity android:name=".EditScheduleActivity"></activity>
        <activity android:name=".EditGroupActivity"></activity>
    </application>

</manifest>